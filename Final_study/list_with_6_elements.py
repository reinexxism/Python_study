n_list = [11, 22, 33, 44, 55, 66]
print(n_list)

# 항목의 개수가 n개인 list의 index는 0부터 시작해서 n-1까지 증가한다.

print(len(n_list)) # list의 요소의 개수를 구하는 function
print(n_list[0]) # list의 첫 번째 항목의 index는 0이다.
print(n_list[1]) # list의 두 번째 항목의 index는 1이다.